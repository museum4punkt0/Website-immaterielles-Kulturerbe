'use strict';

/**
 * cluster-teaser-section controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cluster-teaser-section.cluster-teaser-section');
