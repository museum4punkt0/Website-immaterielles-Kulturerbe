'use strict';

/**
 * cluster-teaser-section router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::cluster-teaser-section.cluster-teaser-section');
