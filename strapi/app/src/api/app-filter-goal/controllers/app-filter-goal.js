'use strict';

/**
 * app-filter-goal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::app-filter-goal.app-filter-goal');
