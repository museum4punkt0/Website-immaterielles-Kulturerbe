'use strict';

/**
 * house-filter-agency router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::house-filter-agency.house-filter-agency');
