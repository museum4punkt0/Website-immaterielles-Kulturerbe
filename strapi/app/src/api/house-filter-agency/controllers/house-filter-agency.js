'use strict';

/**
 * house-filter-agency controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::house-filter-agency.house-filter-agency');
