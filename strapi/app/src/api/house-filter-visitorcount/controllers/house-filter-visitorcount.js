'use strict';

/**
 * house-filter-visitorcount controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::house-filter-visitorcount.house-filter-visitorcount');
